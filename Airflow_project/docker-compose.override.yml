services:
  mongodb:
    image: mongo:7.0
    container_name: mongo
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: geniaer
      MONGO_INITDB_ROOT_PASSWORD: 5R81vGbbEgUBoGQa
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
